# Example environment for Cloud VM

# App entrypoint (relative to repo root)
# Options: PPO_LIVE_TRADER.py, PPO_LIVE_MONITOR.py, PPO_UNIFIED_MONITOR.py, etc.
APP_ENTRY=PPO_LIVE_TRADER.py

# Virtual environment directory name (relative to repo root)
VENV_DIR=venv

# Logging / runtime
PYTHONUNBUFFERED=1
LOG_LEVEL=INFO
TZ=Asia/Taipei

# Symbols & scanning
# Path to your symbols file (optional); common fallbacks are auto-detected if omitted
# SYMBOLS_FILE=capital_symbols_all.txt
# Limit how many symbols to load (default 40 to avoid overload)
MAX_SYMBOLS=40
# Process symbols in batches per loop (0 = all per loop)
BATCH_SIZE=0

# Broker/API credentials (example; fill with your real keys)
# BROKER_API_KEY=your_key
# BROKER_API_SECRET=your_secret
# API_BASE_URL=https://api.example.com
